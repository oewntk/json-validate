{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://oewntk.githumb.io/schema/data1",
  "title": "flat one data format",
  "description": "flat one data format (lex does not embed sense)",
  "$ref": "#/$defs/grouped_by_object_type",
  "$defs": {
    "grouped_by_object_type": {
      "type": "object",
      "properties": {
        "lexes": {
          "type": "array",
          "items": {
            "$ref": "defs-data.json#/$defs/lex"
          }
        },
        "synsets": {
          "type": "array",
          "items": {
            "$ref": "defs-data.json#/$defs/synset"
          }
        },
        "senses": {
          "type": "array",
          "items": {
            "$ref": "defs-data.json#/$defs/sense"
          }
        }
      },
      "required": [
        "lexes",
        "synsets",
        "senses"
      ],
      "additionalProperties": false
    }
  }
}